# Etapa 2 — Bloco C: Consolidacao Funcional para Validacao com as Areas

## 1. Escopo e fontes

### Arquivos consultados

- `/Users/lucasnogueira/Documents/3A/Connect-backup/docs/workflows_new/CHECKLIST_OPERACIONAL_ETAPA_2.md`
- `/Users/lucasnogueira/Documents/3A/Connect-backup/docs/workflows_new/ETAPA2_BLOCO_A_MAPA_FUNCIONAL_CONFIGURADO.md`
- `/Users/lucasnogueira/Documents/3A/Connect-backup/docs/workflows_new/ETAPA2_BLOCO_B_FLUXO_OPERACIONAL_IMPLEMENTADO.md`
- `/Users/lucasnogueira/Documents/3A/Connect-backup/src/scripts/results/workflow_samples.json`
- `/Users/lucasnogueira/Documents/3A/Connect-backup/src/scripts/results/workflowDefinitions.json`
- `/Users/lucasnogueira/Documents/3A/Connect-backup/README.md`

### Escopo da evidencia

- `workflow_samples.json` contem `33` chaves de `type`.
- `workflowDefinitions.json` contem `33` workflows.
- A interseccao entre os `33` `types` observados e os `33` workflows definidos e completa: `33/33`.
- O arquivo de samples traz `1` amostra por `type`.

### Fato comprovado

- Na amostra atual:
  - `17` documentos estao `isArchived = false`
  - `16` documentos estao `isArchived = true`
  - `21` documentos possuem `assignee`
  - `12` documentos nao possuem `assignee`
  - `9` documentos possuem `actionRequests`
  - `2` documentos possuem `formData` vazio
- As divergencias que mais impactam a validacao funcional atual concentram-se em:
  - workflows cuja configuracao atual esta ambigua (`status.id` ou `field.id` duplicado)
  - workflows cujo sample observado ainda percorre um fluxo diferente do cadastrado hoje
  - workflows cujo formulario atual nao esta descrito pelo JSON

### Inferencia

- Quando um sample atual usa etapas fora da definicao vigente, ele deixa de servir como prova do fluxo atual e passa a servir como prova de ambiguidade funcional a validar com a area.

### Ponto em aberto

- Como existe apenas `1` amostra por `type`, um workflow alinhado ao sample nao prova cobertura completa; ele apenas mostra que, na amostra observada, nao apareceu conflito funcional relevante.

## 2. Consolidacao funcional por area

### Facilities e Suprimentos

| workflow | fluxo atual consolidado | divergencia funcional relevante | classificacao | o que validar com a area |
| --- | --- | --- | --- | --- |
| `Manutenção / Solicitações Gerais` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `setor_area`, `impacto`, `descricao_detalhada`, `centrodecusto`, `email`; o runtime envia primeiro para `ownerEmail = stefania.otoni@3ainvestimentos.com.br`; depois pode haver `assignee` manual; a amostra `requestId 0258` percorre `solicitacao_aberta -> em_analise -> em_andamento -> finalizado`, com `assignee = Luis Henrique Dusanek Guedes`; o fim operacional hoje e `finalizado`, com arquivamento manual separado. | Nenhuma divergencia funcional relevante na amostra `0258`. | `-` | Confirmar apenas se a atribuicao manual ao longo do atendimento faz parte da operacao desejada da area. |
| `Solicitação de Compras` | Fato: abrem apenas os IDs listados no cadastro; entram `centro_custo`, `nome_sobrenome`, `email`, `item_compra`, `quantidade`, `motivo`, `link_produto`, `impacto` e opcional `anexos`; configuracao atual sugere `Solicitação Aberta -> Em análise -> Em aprovação - FIN -> Em execução -> Finalizado`; runtime atual mandaria primeiro para `ownerEmail = stefania.otoni@3ainvestimentos.com.br`, depois para eventual `assignee`; hoje o termino configurado e `finalizada`, com arquivamento manual depois. | Fato observado na amostra `requestId 0053`: `history` contem `ciencia_solicitante`; `actionRequests` existem em `emaprovacao_fin` e `em_execucao`, embora a definicao atual nao tenha `action` nesses statuses; isso impede usar o sample como prova do fluxo atual. Escopo: evidencia de `1` sample. | `duvida_de_negocio` | Validar com Facilities e Financeiro se `Em aprovação - FIN` deve continuar apenas como status manual ou se ainda exige aprovacao/ciencia formal. |
| `Solicitação de Suprimentos` | Fato: abrem apenas os IDs listados no cadastro; entram `nome_sobrenome`, `email`, `setor_area`, `impacto`, `anexo_planilha` e opcionais `observacoes`, `centrodecusto`; o fluxo configurado e `solicitacao_aberta -> em_analise -> em_andamento -> finalizado`; na abertura vai para `ownerEmail = stefania.otoni@3ainvestimentos.com.br`; depois pode ser assumido por `assignee`; a amostra `requestId 0261` chega a `finalizado` com `assignee = Guilherme Oliveira Costa`. | Nenhuma divergencia funcional relevante na amostra `0261`. | `-` | Confirmar se o fechamento da area depende apenas de concluir o status ou tambem de arquivar manualmente o item depois. |

### Financeiro

| workflow | fluxo atual consolidado | divergencia funcional relevante | classificacao | o que validar com a area |
| --- | --- | --- | --- | --- |
| `Solicitação de Pagamentos ` | Fato: abrem apenas os IDs listados no cadastro; o runtime manda o item para `ownerEmail = pablo.costa@3ariva.com.br`; o status inicial e `em_analise`; depois o fluxo declarado e `etapa_2 -> finalizado`; a amostra `requestId 0637` confirma abertura em `em_analise`, sem `assignee`, com `history` de uma entrada. | Fato observado: a definicao atual tem `0` `fields` e a amostra `0637` tambem tem `formData = {}`. Escopo: evidencia de `1` sample, mas triangulada com o cadastro atual. | `duvida_de_negocio` | Validar se este workflow depende de formulario externo ou processo manual fora do Connect, porque o JSON atual nao descreve quais informacoes entram. |

### Gente e Comunicação

| workflow | fluxo atual consolidado | divergencia funcional relevante | classificacao | o que validar com a area |
| --- | --- | --- | --- | --- |
| `Alteração Cadastral` | Fato: `allowedUserIds = all`; entram `campo_1`, `campo_2`, `campo_3`, `campo_4`, `campo_5`, `campo_6`, `campo_7`, `campo_8`, `campo_9`, `campo_14`, `campo_15`, `campo_16`, `campo_19`, `campo_20`, `campo_21` e opcionais `campo_10`, `campo_12`, `campo_13`; o runtime envia para `ownerEmail = fernanda.adami@3ainvestimentos.com.br`; depois pode haver `assignee`; a amostra `requestId 0542` chegou a `finalizado`, com `assignee = Dalcion Franco Amaral`, e os campos ausentes na amostra sao opcionais na definicao atual. | Nenhuma divergencia funcional relevante na amostra `0542`. | `-` | Confirmar se o fluxo pode continuar linear, sem checkpoint adicional, e com atribuicao manual para operacao. |
| `Alteração de Cargo / Remuneração / Time ou Equipe` | Fato: abrem apenas os IDs listados no cadastro; entram dados do lider e do colaborador, mais anexo e possiveis novos cargo/equipe/remuneracao; o fluxo declarado e `solicitacao_aberta -> em_analise -> em_execucao -> finalizado`; a fila inicial vai para `ownerEmail = fernanda.adami@3ainvestimentos.com.br`; depois pode haver `assignee`; a amostra `requestId 0421` esta `finalizado`. | Fato observado: a configuracao atual usa `field.id = email` duas vezes, para lider e colaborador; no runtime, IDs duplicados se sobrepoem na persistencia do `formData`. Escopo: evidencia estrutural do cadastro atual; o sample `0421` nao resolve a ambiguidade. | `configuracao_incorreta` | Validar com Gente qual e o nome canonico de cada e-mail e se o formulario precisa de dois campos distintos obrigatorios no estado atual. |
| `Análise Pré-Desligamento (Acesso líderes)` | Fato: abrem apenas os IDs listados no cadastro; entram dados do lider, do colaborador desligado, tipo de desligamento, `cod_xp`, observacoes e data; o cadastro descreve uma sequencia multi-etapa BI -> Financeiro -> Jurídico -> Governança -> reanalise Jurídico -> Governança final; o runtime sempre envia a abertura para `ownerEmail = barbara@3ainvestimentos.com.br`; nos statuses com `action`, terceiros podem executar; a amostra `requestId 0663` mostra apenas abertura em `em_analise`, sem `assignee`. | Fato observado: `6` etapas diferentes usam `status.id = em_analise`; o runtime atual localiza etapas por `status.id`, logo nao consegue distinguir claramente BI, Financeiro, Jurídico e Governança. Escopo: evidencia do cadastro atual, triangulada com o runtime; o sample `0663` so comprova o estado inicial observado. | `configuracao_incorreta` | Validar com Gente, Financeiro, Jurídico e Governança qual e a ordem canonica das etapas e quais IDs de status devem existir para representar o fluxo ativo. |
| `Cadastro de Novos Entrantes - Demais Áreas ` | Fato: abrem apenas os IDs listados no cadastro; entram dados do novo entrante, contratacao, telefone, cargo, lider, equipe, data, `anexo` e observacoes; configuracao atual descreve `Solicitação Aberta -> Enviado ao TIME de Gente -> Criar acessos/equipamentos - TI -> Finalizado`; o runtime manda primeiro para `ownerEmail = fernanda.adami@3ainvestimentos.com.br`; depois pode haver `assignee`; quando entra em `acessos_ti`, `DFZ2` pode receber `acknowledgement`; a amostra `requestId 0238` esta `finalizado`, com `assignee = Dalcion Franco Amaral`. | Fato observado na amostra `0238`: `history` ainda contem `em_analise`, `ciente_fin` e `cadastro_plataformas`; `actionRequests` incluem `ciente_fin`, ausente na configuracao atual. Escopo: evidencia de `1` sample que impede usar o historico observado como prova do fluxo atual. | `duvida_de_negocio` | Validar se o fluxo oficial atual e apenas `Gente -> TI`, ou se Financeiro e plataformas ainda precisam aparecer na regra de negocio vigente. |
| `Cadastro de Novos Entrantes - Associado` | Fato: abrem apenas os IDs listados no cadastro; entram dados do novo entrante, lider, equipe, data, `anexo`, observacoes e `pf_abaixo_pj`; a configuracao atual declara `Em aberto -> Enviado ao TIME de Gente -> Enviado ao TI (Acessos) -> Enviado ao Jurídico -> Finalizado`; o runtime envia a abertura para `ownerEmail = fernanda.adami@3ainvestimentos.com.br`; `DFZ2` pode dar ciencia em `enviar_ti`; `LBC, AMP2, DSC2` podem dar ciencia em `enviar_juridico`; a amostra `requestId 0375` tem `assignee = Dalcion Franco Amaral`. | Fato observado na amostra `0375`: o `status` atual e `enviar_financeiro`, inexistente na definicao vigente; ha `actionRequests` em `enviado_gente`, `enviar_financeiro` e `enviar_juridico`. Escopo: evidencia de `1` sample que conflita diretamente com o fluxo cadastrado hoje. | `duvida_de_negocio` | Validar se Financeiro ainda participa do fluxo oficial de novos entrantes associados ou se o caminho atual deve ser somente `Gente -> TI -> Jurídico`. |
| `Comprovação ANCORD` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `email`, `setor_area`, `contratacao_via`, `comprovante_ancord` e opcional `codigo_xp`; a configuracao atual declara `Solicitação Aberta -> Em análise -> Análise - Jurídico -> Análise - Gestão -> Finalizada`; o runtime manda a abertura para `ownerEmail = fernanda.adami@3ainvestimentos.com.br`; depois pode haver `assignee`; a amostra `requestId 0216` esta `finalizada`, com `assignee = Dalcion Franco Amaral`. | Fato observado na amostra `0216`: existem `actionRequests` em `em_analise`, `analise_juridico` e `analise_gestao`, embora a definicao atual nao declare `action` nesses statuses. Escopo: evidencia de `1` sample. | `duvida_de_negocio` | Validar se Jurídico e Gestão ainda precisam responder checkpoints formais ou se o fluxo atual realmente virou linear sem `action`. |
| `Fale com a GENTE` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `email`, `setor_area`, `descricao_detalhada`; a abertura vai para `ownerEmail = fernanda.adami@3ainvestimentos.com.br`; depois pode haver `assignee`; a amostra `requestId 0169` percorre ate `finalizado`, com `assignee = Dalcion Franco Amaral`. | Nenhuma divergencia funcional relevante na amostra `0169`. | `-` | Confirmar apenas se a leitura atual de owner + assignee manual e suficiente para a operacao da area. |
| `Serviços de Plano de Saúde ` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `email`, `setor_area`, `tipo_solicitacao` e opcional `descricao_detalhada`; o fluxo atual e `solicitacao_aberta -> em_analise -> finalizado`; a abertura vai para `ownerEmail = fernanda.adami@3ainvestimentos.com.br`; a amostra `requestId 0061` chegou a `finalizado`, com `assignee = Mariana Vargas Teixeira`. | Nenhuma divergencia funcional relevante na amostra `0061`. | `-` | Confirmar se o fluxo deve continuar sem etapa intermediaria adicional e sem `action`. |
| `Solicitação Desligamento - Demais áreas (Não comerciais)` | Fato: abrem apenas os IDs listados no cadastro; entram dados do lider, do colaborador desligado, motivo, tipo de cargo, tipo de desligamento, data e opcionais `cod_xp`, `observacoes`; a abertura vai para `ownerEmail = fernanda.adami@3ainvestimentos.com.br`; depois o fluxo configurado e `em_analise -> em_andamento -> desligamento_gente -> desligamento_ti -> desligamento_bi -> enviar_adm -> finalizado`; ha `approval` em `em_andamento` e `acknowledgement` em `desligamento_gente`, `desligamento_ti`, `desligamento_bi`, `enviar_adm`; a amostra `requestId 0242` esta `finalizado`, com `assignee = Dalcion Franco Amaral`. | Fato observado na amostra `0242`: `actionRequests` existem exatamente nos mesmos statuses configurados hoje; o sample confirma o desenho multi-area atual, mas o runtime continua exigindo avancos manuais entre as etapas. Escopo: evidencia de `1` sample alinhado ao cadastro vigente. | `comportamento_intencional` | Validar se o negocio aceita que cada checkpoint so registre aprovacao/ciencia e que a mudanca de status continue manual entre as etapas. |
| `Solicitação de Abertura de Vaga` | Fato: abrem apenas os IDs listados no cadastro; entram `nome_sobrenome`, `setor_area`, `descricao_vaga`, `prazo_contratacao`, `email`; o fluxo atual configurado e linear `solicitacao_aberta -> em_analise -> em_execucao -> finalizado`; a abertura vai para `ownerEmail = fernanda.adami@3ainvestimentos.com.br`; depois pode haver `assignee`; a amostra `requestId 0021` esta `finalizado`, com `assignee = Mariana Vargas Teixeira`. | Fato observado na amostra `0021`: existe `actionRequests` em `em_execucao`, com nota de ciencia nao prevista na definicao atual. Escopo: evidencia de `1` sample que aponta fluxo observado diferente do cadastrado. | `duvida_de_negocio` | Validar se a area ainda precisa de uma ciencia formal em `em_execucao` ou se o fluxo vigente realmente e linear. |
| `Solicitação de Férias / Ausência / Compensação de horas` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `email`, `setor_area`, `tipo_solicitacao`, `data_inicio`, `data_fim`, `descricao_detalhada`; a abertura vai para `ownerEmail = fernanda.adami@3ainvestimentos.com.br`; o status `em_execucao` gera `acknowledgement` para `DFA`; depois alguem precisa mover manualmente para `finalizado`; a amostra `requestId 0187` esta `finalizado`, com `assignee = Dalcion Franco Amaral` e `actionRequests` em `em_execucao`. | Nenhuma divergencia funcional relevante entre cadastro, runtime e amostra `0187`; a amostra confirma a existencia de `acknowledgement` em `em_execucao`. | `comportamento_intencional` | Validar apenas se a ciencia de Gente em `em_execucao` deve permanecer como checkpoint do fluxo atual. |

### Governança

| workflow | fluxo atual consolidado | divergencia funcional relevante | classificacao | o que validar com a area |
| --- | --- | --- | --- | --- |
| `Espelhamento - Caso Único ` | Fato: abrem apenas os IDs listados no cadastro; entram `nome_sobrenome`, `tipo_solicitacao`, `campo_3`, `campo_4`, `campo_5`, `campo_6`, `descricao_detalhada`; apesar de estar em `Governança`, a abertura vai para `ownerEmail = ti@3ariva.com.br`; o fluxo atual e linear `solicitacao_aberta -> em_analise -> em_execucao -> finalizado`; a amostra `requestId 0474` esta em `solicitacao_aberta`, sem `assignee`. | Fato observado na amostra `0474`: `ownerEmail = marcio.peixoto@3ariva.com.br`, diferente da definicao vigente. Escopo: evidencia de `1` sample; o runtime atual explica snapshot de owner na criacao, mas nao define qual owner atual deve ser validado com a area. | `duvida_de_negocio` | Validar se o owner funcional vigente do workflow continua em TI ou se ainda existe ownership operacional em Governança/Marcio. |
| `Espelhamento - Em lote` | Fato: abrem apenas os IDs listados no cadastro; entram `nome_sobrenome`, `tipo_solicitacao`, dois campos `email_lider` e `anexo_planilha`; a abertura vai para `ownerEmail = ti@3ariva.com.br`; o fluxo atual e linear; a amostra `requestId 0573` esta em `solicitacao_aberta`, sem `assignee`, com `isArchived = true`. | Fato observado: a definicao atual repete `field.id = email_lider` e a amostra `0573` ainda mostra `_uploadErrors` em `formData`, o que prova que o runtime insere metadado tecnico quando upload falha. Escopo: evidencia estrutural do cadastro e `1` sample real. | `configuracao_incorreta` | Validar quais sao os dois e-mails corretos do fluxo em lote e se `_uploadErrors` deve ser ignorado pela area como metadado tecnico. |

### Marketing

| workflow | fluxo atual consolidado | divergencia funcional relevante | classificacao | o que validar com a area |
| --- | --- | --- | --- | --- |
| `Arte / Material gráfico` | Fato: `allowedUserIds = all`; entram dados de briefing, publico, objetivo, texto, versoes, categoria, prazo e duas ocorrencias de `imagem_referencia`; a abertura vai para `ownerEmail = joao.pompeu@3ainvestimentos.com.br`; depois pode haver `assignee`; a amostra `requestId 0075` esta `finalizado`, com `assignee = João Pedro Pompeu Ulhoa`. | Fato observado: o cadastro atual repete `field.id = imagem_referencia`; na amostra `0075`, `ownerEmail = barbara.fiche@3ainvestimentos.com.br`, diferente da definicao atual. Escopo: evidencia estrutural do cadastro e `1` sample real. | `duvida_de_negocio` | Validar quem e o owner funcional vigente e se existem mesmo duas capturas de referencia no formulario atual ou apenas um campo deveria permanecer. |
| `Assinatura de e-mail; Cartão de visita; Cartão de visita digital` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `setor_area`, `impacto`, `descricao_detalhada`, `necessario`, `cargo_area`, `email`, `telefone`, `filial`; a abertura vai para `ownerEmail = joao.pompeu@3ainvestimentos.com.br`; depois pode haver `assignee`; a amostra `requestId 0080` esta `finalizado`, com `assignee = João Pedro Pompeu Ulhoa`. | Fato observado na amostra `0080`: `ownerEmail = barbara.fiche@3ainvestimentos.com.br`, diferente da definicao atual. Escopo: evidencia de `1` sample. | `duvida_de_negocio` | Validar com Marketing se o owner atual da regra funcional e `joao.pompeu@3ainvestimentos.com.br` ou se a area ainda opera com owner anterior. |
| `Ações Marketing` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `setor_area`, `impacto`, `descricao_detalhada`, `prazo`; a abertura vai para `ownerEmail = barbara.fiche@3ainvestimentos.com.br`; o fluxo atual configurado e linear; a amostra `requestId 0310` ficou em `solicitacao_aberta`, sem `assignee`. | Fato observado na amostra `0310`: `formData = {}` mesmo com `5` campos obrigatorios configurados; isso e compativel com o runtime em duas fases da criacao. Escopo: evidencia de `1` sample, triangulada com o comportamento do runtime. | `bug_provavel` | Validar com Marketing se registros sem formulario devem ser tratados como erro tecnico, rascunho invalido ou solicitacao valida. |
| `Evento` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `email`, `setor_area`, `impacto`, `descricao_detalhada`, `objetivo`, `publico_alvo`, `quantidade_pessoas`, `horario`, `data`, `nome_evento`, `formato`; a abertura vai para `ownerEmail = barbara.fiche@3ainvestimentos.com.br`; a amostra `requestId 0625` esta em `solicitacao_aberta`, sem `assignee`, com `formData` coerente com o cadastro. | Nenhuma divergencia funcional relevante na amostra `0625`. | `-` | Confirmar apenas se a fila deve continuar com owner unico de Marketing ate atribuicao manual. |
| `Revisão de materiais e Apresentações` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `area`, `material`, `observacao`; o fluxo nasce em `em_aberto`, nao em `solicitacao_aberta`; a abertura vai para `ownerEmail = joao.pompeu@3ainvestimentos.com.br`; a amostra `requestId 0498` esta em `em_aberto`, sem `assignee`. | Nenhuma divergencia funcional relevante na amostra `0498`. | `-` | Confirmar se `Em aberto` deve permanecer como primeiro status canonico do workflow atual. |
| `Solicitação de Patrocínios` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `email`, `setor_area`, `impacto`, `descricao_detalhada`, `hora`, `data`, `valor`, `beneficios`, `apresentacao`; a abertura vai para `ownerEmail = barbara.fiche@3ainvestimentos.com.br`; a amostra `requestId 0211` chegou a `finalizado`, com `assignee = Barbara Fiche Girao`. | Nenhuma divergencia funcional relevante na amostra `0211`. | `-` | Confirmar se o handoff atual continua `owner -> assignee manual -> finalizado`, sem checkpoint adicional. |
| `Sugestão 3A RIVA Store` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `email`, `setor_area`, `impacto`, `descricao_detalhada`; a abertura vai para `ownerEmail = barbara.fiche@3ainvestimentos.com.br`; a amostra `requestId 0410` chegou a `finalizado`, sem `assignee`. | Nenhuma divergencia funcional relevante na amostra `0410`. | `-` | Confirmar apenas se esse workflow deve continuar sem `action` e sem atribuicao obrigatoria. |

### TI

| workflow | fluxo atual consolidado | divergencia funcional relevante | classificacao | o que validar com a area |
| --- | --- | --- | --- | --- |
| `Alteração no E-mail XP` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `email`, `setor_area`, `descricao_detalhada`, `impacto`; a abertura vai para `ownerEmail = ti@3ariva.com.br`; depois pode haver `assignee`; a amostra `requestId 0268` esta em `em_execucao`, com `assignee = Danilo Freitas Zegrini`. | Nenhuma divergencia funcional relevante na amostra `0268`. | `-` | Confirmar apenas se a passagem `owner -> assignee -> finalizado` reflete o fluxo atual esperado por TI. |
| `Padronização de E-mail  - Código XP` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `email`, `setor_area`, `cod_xp`, `contratacao_via`; o cadastro atual declara fluxo linear `solicitacao_aberta -> em_analise -> em_execucao -> finalizado`; a abertura vai para `ownerEmail = ti@3ariva.com.br`; a amostra `requestId 0298` tem `assignee = Danilo Freitas Zegrini`. | Fato observado na amostra `0298`: `status = ciente_gestao`; existem `actionRequests` em `ciente_gestao` e `em_execucao`, embora a definicao atual nao tenha essas etapas. Escopo: evidencia de `1` sample que conflita diretamente com o fluxo cadastrado. | `duvida_de_negocio` | Validar se Gestao ainda precisa dar ciencia nesse workflow ou se o fluxo oficial atual realmente e linear em TI. |
| `Problemas de Hardware` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `email`, `setor_area`, `descricao_detalhada`, `impacto`; a abertura vai para `ownerEmail = ti@3ariva.com.br`; a amostra `requestId 0343` chegou a `finalizado`, sem `assignee`. | Nenhuma divergencia funcional relevante na amostra `0343`. | `-` | Confirmar apenas se o fluxo continua linear sem `action`. |
| `Problemas de Rede` | Fato: `allowedUserIds = all`; entram `nome`, `sobrenome`, `email`, `setor_area`, `descricao_detalhada`, `impacto`; a abertura vai para `ownerEmail = ti@3ariva.com.br`; a amostra `requestId 0138` chegou a `finalizado`, sem `assignee`. | Nenhuma divergencia funcional relevante na amostra `0138`. | `-` | Confirmar apenas se a area aceita manter o fluxo simples sem checkpoints adicionais. |
| `Problemas de Software` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `email`, `setor_area`, `descricao_detalhada`, `impacto`; a abertura vai para `ownerEmail = ti@3ariva.com.br`; a amostra `requestId 0076` chegou a `finalizado`, com `assignee = Luis Henrique Dusanek Guedes`. | Nenhuma divergencia funcional relevante na amostra `0076`. | `-` | Confirmar apenas se o uso de `assignee` manual representa a regra operacional desejada de TI. |
| `Reset de Senha` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `email`, `setor_area`, `rese_senha`; a abertura vai para `ownerEmail = ti@3ariva.com.br`; a amostra `requestId 0190` chegou a `finalizado`, com `assignee = Danilo Freitas Zegrini`. | Nenhuma divergencia funcional relevante na amostra `0190`; o ponto estrutural e que o workflow nao aparece no `workflowOrder` da area TI. Escopo: evidencia do cadastro, nao do sample. | `duvida_de_negocio` | Validar se `Reset de Senha` deve continuar visivel como workflow ativo principal da area TI. |
| `Solicitação de Compra - Equipamento` | Fato: `allowedUserIds = all`; entram `nome_completo`, `email`, `setor_area`, `item_compra_ti`, `quantiadade_compra_ti` e opcional `link_compra_ti`; a abertura vai para `ownerEmail = ti@3ariva.com.br`; o cadastro atual pretende `Solicitação Aberta -> Em Análise -> Em aprovação -> Em execução -> Finalizado`, mas usa `status.id = em_execucao` nas duas etapas intermediarias; a amostra `requestId 0192` esta em `em_execucao`, com `assignee = Danilo Freitas Zegrini`. | Fato observado: a configuracao atual tem `status.id` duplicado; a amostra `0192` tem `historyCount = 46`, repetindo `em_execucao` e registrando nota automatica de `Aprovação TI / FIN`, inexistente na definicao atual. Escopo: evidencia estrutural do cadastro e `1` sample real que torna ambigua a regra vigente. | `configuracao_incorreta` | Validar com TI e Financeiro se existem duas etapas distintas no fluxo atual e quais IDs canonicos devem representar `Em aprovação` e `Em execução`. |
| `Solicitação de Compra - Software/Sistema` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `email`, `descricao`, `recorrencia`, `motivo`, `area` e opcional `nome`; o workflow nasce em `em_analise`; a abertura vai para `ownerEmail = ti@3ariva.com.br`; a amostra `requestId 0536` chegou a `finalizado`, com `assignee = Pierre Alves`. | Nenhuma divergencia funcional relevante na amostra `0536`. | `-` | Confirmar apenas se o fluxo deve continuar iniciando direto em `Em Análise`. |
| `Sugestões 3A RIVA Connect` | Fato: `allowedUserIds = all`; entram `nome_sobrenome`, `email`, `setor_area`, `descricao_detalhada`; a abertura vai para `ownerEmail = matheus@3ainvestimentos.com.br`; a amostra `requestId 0460` chegou a `finalizado`, com `assignee = Matheus Vinicius Souza Mattos`. | Nenhuma divergencia funcional relevante na amostra `0460`. | `-` | Confirmar apenas se o owner atual do fluxo de sugestoes continua centralizado nessa conta. |

## 3. Divergencias relevantes que afetam a validacao funcional atual

| workflow | type / requestId ou amostra | fato observado | classificacao | observacao sobre o escopo da evidencia |
| --- | --- | --- | --- | --- |
| `Ações Marketing` | `type = Ações Marketing`, `requestId = 0310` | `formData = {}` apesar de `5` fields obrigatorios no cadastro atual | `bug_provavel` | evidencia de `1` sample, triangulada com o fluxo em duas fases do runtime |
| `Solicitação de Pagamentos ` | `type = Solicitação de Pagamentos `, `requestId = 0637` | cadastro atual sem `fields` e sample sem `formData` | `duvida_de_negocio` | evidencia estrutural do cadastro atual, apoiada por `1` sample |
| `Solicitação de Compra - Equipamento` | `type = Solicitação de Compra - Equipamento`, `requestId = 0192` | `status.id` duplicado no cadastro atual e historico observado com repeticoes de `em_execucao` e nota de aprovacao nao descrita hoje | `configuracao_incorreta` | evidencia do cadastro atual + `1` sample real |
| `Análise Pré-Desligamento (Acesso líderes)` | `type = Análise Pré-Desligamento (Acesso líderes)`, amostra `0663` | `6` etapas configuradas com `status.id = em_analise`, o que impede distinguir a ordem canonica no runtime atual | `configuracao_incorreta` | evidencia do cadastro atual; o sample `0663` so mostra o estado inicial |
| `Alteração de Cargo / Remuneração / Time ou Equipe` | `type = Alteração de Cargo / Remuneração / Time ou Equipe`, amostra `0421` | `field.id = email` duplicado entre lider e colaborador | `configuracao_incorreta` | evidencia do cadastro atual; a amostra nao resolve qual campo deve prevalecer |
| `Espelhamento - Em lote` | `type = Espelhamento - Em lote`, `requestId = 0573` | `field.id = email_lider` duplicado e metadado `_uploadErrors` presente no `formData` | `configuracao_incorreta` | evidencia do cadastro atual + `1` sample real |
| `Cadastro de Novos Entrantes - Associado` | `type = Cadastro de Novos Entrantes - Associado`, `requestId = 0375` | sample usa `enviar_financeiro`, ausente na definicao atual | `duvida_de_negocio` | evidencia de `1` sample que impede validar o fluxo atual so com base nos dados observados |
| `Cadastro de Novos Entrantes - Demais Áreas ` | `type = Cadastro de Novos Entrantes - Demais Áreas `, `requestId = 0238` | sample usa `ciente_fin` e `cadastro_plataformas`, ausentes na definicao atual | `duvida_de_negocio` | evidencia de `1` sample |
| `Solicitação de Compras` | `type = Solicitação de Compras`, `requestId = 0053` | sample registra `actionRequests` em statuses sem `action` na definicao atual | `duvida_de_negocio` | evidencia de `1` sample |
| `Comprovação ANCORD` | `type = Comprovação ANCORD`, `requestId = 0216` | sample registra `actionRequests` em `em_analise`, `analise_juridico`, `analise_gestao`, ausentes na definicao atual | `duvida_de_negocio` | evidencia de `1` sample |
| `Solicitação de Abertura de Vaga` | `type = Solicitação de Abertura de Vaga`, `requestId = 0021` | sample registra checkpoint de ciencia em `em_execucao`, ausente na definicao atual | `duvida_de_negocio` | evidencia de `1` sample |
| `Padronização de E-mail  - Código XP` | `type = Padronização de E-mail  - Código XP`, `requestId = 0298` | sample usa `ciente_gestao`, ausente na definicao atual | `duvida_de_negocio` | evidencia de `1` sample |
| `Assinatura de e-mail; Cartão de visita; Cartão de visita digital` | `type = Assinatura de e-mail; Cartão de visita; Cartão de visita digital`, `requestId = 0080` | `ownerEmail` do sample difere do owner atual da definicao | `duvida_de_negocio` | evidencia de `1` sample; o runtime explica snapshot de owner, mas nao define owner vigente |
| `Arte / Material gráfico` | `type = Arte / Material gráfico`, `requestId = 0075` | `ownerEmail` do sample difere do owner atual da definicao | `duvida_de_negocio` | evidencia de `1` sample |
| `Espelhamento - Caso Único ` | `type = Espelhamento - Caso Único `, `requestId = 0474` | `ownerEmail` do sample difere do owner atual da definicao | `duvida_de_negocio` | evidencia de `1` sample |

## 4. Fechamento do Bloco C

### Fato comprovado

- Todos os `33` workflows ativos definidos hoje foram confrontados contra:
  - configuracao
  - runtime
  - `1` sample real por `type`
- Este documento foi organizado por area de negocio e por workflow ativo.

### Inferencia

- Para a validacao com as areas, o risco principal nao e “legado historico antigo”, e sim:
  - workflow ativo cuja definicao atual esta ambigua
  - workflow ativo cujo sample real ainda reflete fluxo diferente do cadastro atual
  - workflow ativo cujo formulario atual nao esta formalizado

### Ponto em aberto

- A etapa ainda depende de validacao humana para decidir quais dos conflitos acima representam:
  - regra vigente que precisa voltar para o cadastro
  - regra antiga que deve ser descartada
  - problema tecnico do fluxo atual
